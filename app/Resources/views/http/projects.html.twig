{% extends 'base.html.twig' %}

{% block body %}
    {% for project in projects %}
        {% for item in project %}
            Project # {{ item.name }}/{{ item.id }}<br>
        {% endfor %}
    {% endfor %}
{% endblock %}

